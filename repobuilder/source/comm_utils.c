#include "configserver.h"
//#include <Ice/Ice.h>
/*
This object is used to load application file and sent to each client as a message
*/
//CommandMessage g_file_message;


//Ice::CommunicatorPtr g_ic;



int	send_cmd2host (dev_task_t* pdev, const app_t * pa, const config_t * config)
{
	/*
	char szip[20] = {0};
	int nRet = RET_SUCCESS;
	int nsock = 0;
	char szbuf [1024] = {0};
	int len = 0;
	int send = 0;
	FILE* fp = 0;
	char szfile[128] = {0};
	usleep (50);
	return nRet;
	*/
	return 1;
}
